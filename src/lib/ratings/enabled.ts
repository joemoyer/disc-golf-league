/**
 * Player skill ratings (Elo + form handicap) — hidden while tuning.
 * Set to `true` to re-enable public/admin UI, auto-recompute on score entry, and maintenance tools.
 * Full implementation remains in `src/lib/ratings/` — do not delete.
 */
export const PLAYER_RATINGS_ENABLED = false;
